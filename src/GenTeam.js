function renderInput (response) {
    return  `    

    ${response.name}
    ${response.id}
    ${response.email}
    ${response.github}
    ${response.onumber}
    ${response.school}
    
    `
};

module.exports = GenTeam;